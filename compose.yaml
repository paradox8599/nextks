services:
  db:
    image: postgres:16
    ports:
      - 5678:5432
    environment:
      POSTGRES_DB: nextks
      POSTGRES_USER: kextks
      POSTGRES_PASSWORD: kextks
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./.keystone/postgres:/var/lib/postgresql/data
